# TodoListApp Use Case Diagram

> 참조: PRD v0.2.0-draft | 작성일: 2026-05-13

---

```mermaid
flowchart LR
    %% ── 액터 ──────────────────────────────────────────
    UA(["👤 비인증 사용자"])
    UB(["👤 인증된 사용자"])

    %% ── 시스템 경계 ────────────────────────────────────
    subgraph SYS["TodoListApp"]

        %% Identity Context
        subgraph IC["Identity Context  (신원 · 인증)"]
            direction TB
            UC01(["UC-01\n회원가입"])
            UC02(["UC-02\n로그인"])
            UC11(["UC-11\n로그아웃"])
            UC03(["UC-03\n개인정보 수정"])
            UC12(["UC-12\n회원 탈퇴"])
        end

        %% Todo Management Context
        subgraph TC["Todo Management Context  (할일 · 카테고리 관리)"]
            direction TB

            subgraph CAT["카테고리 관리"]
                UC09(["UC-09\n카테고리 추가"])
                UC10(["UC-10\n카테고리 삭제"])
            end

            subgraph TODO["할일 관리"]
                UC08(["UC-08\n할일 목록 조회"])
                UC04(["UC-04\n할일 등록"])
                UC05(["UC-05\n할일 수정"])
                UC06(["UC-06\n할일 완료 처리"])
                UC07(["UC-07\n할일 삭제"])
            end
        end

        %% include / extend 관계
        UC04 -. "«include»\n카테고리 선택" .-> UC08
        UC10 -. "«include»\n할일 재배정" .-> UC08
        UC12 -. "«include»\n비밀번호 확인" .-> UC03
    end

    %% ── 액터 → 유스케이스 연결 ──────────────────────────
    UA --> UC01
    UA --> UC02

    UB --> UC11
    UB --> UC03
    UB --> UC12

    UB --> UC09
    UB --> UC10

    UB --> UC08
    UB --> UC04
    UB --> UC05
    UB --> UC06
    UB --> UC07

    %% ── 스타일 ─────────────────────────────────────────
    classDef actor     fill:#e8f4fd,stroke:#2980b9,stroke-width:2px,color:#1a5276,font-weight:bold
    classDef usecase   fill:#fdfefe,stroke:#7f8c8d,stroke-width:1px,color:#2c3e50
    classDef boundary  fill:#f8f9fa,stroke:#bdc3c7,stroke-width:1px

    class UA,UB actor
    class UC01,UC02,UC03,UC04,UC05,UC06,UC07,UC08,UC09,UC10,UC11,UC12 usecase
```

---

## 액터 정의

| 액터 | 설명 | 접근 가능 UC |
|------|------|------------|
| 비인증 사용자 | 회원가입 또는 로그인 전 상태의 사용자 | UC-01, UC-02 |
| 인증된 사용자 | JWT 발급 후 인증된 상태의 사용자 | UC-03 ~ UC-12 |

---

## 관계 설명

| 관계 | 유형 | 설명 |
|------|------|------|
| UC-04 → UC-08 | «include» | 할일 등록 시 카테고리 선택을 위해 카테고리 목록 조회가 항상 포함됨 |
| UC-10 → UC-08 | «include» | 카테고리 삭제 시 소속 할일을 기본 카테고리로 재배정하는 처리가 항상 포함됨 (BR-07, 단일 트랜잭션) |
| UC-12 → UC-03 | «include» | 회원 탈퇴 시 본인 확인을 위한 비밀번호 검증이 항상 포함됨 |

---

## Bounded Context 경계

| Context | 포함 UC | 핵심 엔티티 |
|---------|---------|-----------|
| Identity Context | UC-01, UC-02, UC-03, UC-11, UC-12 | User, RefreshToken |
| Todo Management Context | UC-04 ~ UC-10 | Todo, Category |
